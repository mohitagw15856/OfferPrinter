# requirements.txt — fallback for users who don't use uv.
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# This mirrors the runtime dependencies in pyproject.toml plus the optional
# web UI. Prefer `uv sync` (which uses the pinned uv.lock) when you can, or
# `pipx install offerprinter` if you just want the CLI.

httpx>=0.27
pydantic>=2.6
python-docx>=1.1
pypdf>=4.2
selectolax>=0.3.21
typer>=0.12
rich>=13.7

# Optional: the Streamlit web UI (`streamlit run app.py`).
streamlit>=1.36

# Optional, for development (lint + tests):
#   pip install ruff pytest pytest-cov
